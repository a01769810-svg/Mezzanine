# meshes/

Custom meshes only. The xArm 6 meshes are referenced via
`package://xarm_description/...` and must NOT be copied here.

Phase 1: no custom meshes are required (everything is rendered as primitives).
